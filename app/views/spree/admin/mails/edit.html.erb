<% content_for :page_title do %>
  <%= link_to Spree.t(:mails), admin_mails_url %> /
<% end %>

<%= render partial: 'spree/admin/shared/error_messages',
  locals: { target: @mail } %>

<%= form_for [:admin, @mail] do |f| %>
  <%= render partial: "form", locals: {f: f} %>
<% end %>
