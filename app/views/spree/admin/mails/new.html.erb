<% content_for :page_title do %>
  <%= link_to Spree.t(:mail), spree.admin_mails_path %> /
  <%= "New Send Mail" %>
<% end %>

<%= render partial: 'spree/admin/shared/error_messages', locals: { target: @mail } %>

<%= form_for [:admin, @mail] do |f| %>
  <%= render partial: "form", locals: {f: f} %>
<% end %>
